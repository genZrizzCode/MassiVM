#!/bin/bash
# PostgreSQL is already installed as postgresql-client in Dockerfile
# For full PostgreSQL server, use Docker or external service
echo "PostgreSQL client installed. For full server, use external PostgreSQL or Docker container." 